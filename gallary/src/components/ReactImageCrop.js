import React, { Component } from 'react';
import ReactCrop from 'react-image-crop';
import 'react-image-crop/lib/ReactCrop.scss';

export default class ReactImageCrop extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
